import { Router } from 'express';

const router = Router();

// TODO: User profile management routes
// GET  /         → Get user profile
// PATCH /        → Update user profile
// DELETE /       → Deactivate account
// PATCH /avatar  → Update avatar

export default router;
