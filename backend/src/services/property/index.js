import { Router } from 'express';

const router = Router();

// TODO: Property service — Listings + Rooms
// POST   /                → Create property (host)
// GET    /                → List properties
// GET    /:id             → Get property details
// PATCH  /:id             → Update property (host)
// DELETE /:id             → Delete property (host)
// POST   /:id/rooms       → Add room to property
// GET    /:id/rooms       → List rooms for property
// PATCH  /:id/rooms/:rid  → Update room
// DELETE /:id/rooms/:rid  → Delete room

export default router;
