import { Router } from 'express';

const router = Router();

// TODO: Media service — Photos + CDN (S3/Cloudinary)
// POST   /upload         → Upload property/room photo
// DELETE /:id            → Delete photo
// PATCH  /:id/cover      → Set as cover photo

export default router;
