import { Router } from 'express';

const router = Router();

// TODO: Search service — Geo search + Filters
// GET /properties    → Search properties with filters
// GET /suggestions   → Auto-complete suggestions
// GET /nearby        → Geo-based nearby properties

export default router;
